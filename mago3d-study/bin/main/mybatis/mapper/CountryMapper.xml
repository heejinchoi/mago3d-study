<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.persistence.CountryMapper">

	<!-- 국가 목록 조회 -->
	<select id="getCountryList" parameterType="com.example.demo.domain.Country" resultType="com.example.demo.domain.Country">
		/* getcountryList*/
		SELECT * FROM country
	</select>
	
</mapper>